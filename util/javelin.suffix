
    if (typeof module !== 'undefined') {
        // export for node
        module.exports = Javelin.getRegistryInstance();
    } else {
        // assign to window
        this.javelin = new Javelin.Registry();
    }
}).apply(this);
